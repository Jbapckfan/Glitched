Level 24 — Clear cache to dissolve a DATA MASS wall (reach + arm a PURGE TERMINAL on a ledge, then free storage / "CAN'T DO THIS?" to purge the unjumpable junk wall blocking the exit).
VERDICT: rework (2/5)
STRENGTHS: The arm-the-terminal gating is a genuinely strong redesign: it kills the old one-tap trivia by requiring a short jump-up traverse before any purge counts, and the cascade-by-stratum dissolve sells "clutter being reclaimed." On iPhone the layout is clean and legible: junk wall, terminal, asides, and EXIT all read clearly, and the panels sit safely clear of the title/pause bands.
CONCERNS: The iPad render directly contradicts the code's own "VOID FIX" comments: instead of a confined vertical zig-zag column centered on size.width/2, the platforms march as a diagonal staircase up-and-right while the spawn sits bottom-left, leaving the entire upper ~50% and center-right of the frame empty white void. Worse, the terminal ledge, the JUNK MASS wall, and the EXIT door are all absent from the visible iPad frame — so the mechanic's payoff and goal are invisible, and the level reads as incomplete/unfair on iPad first encounter. iPhone is fine; the wide-canvas composition is the failure.
CLUE/HINT: Good on both axes. The t=0 intro panel ("JUNK MASS WALLS OFF THE EXIT / JUMP TO THE PURGE TERMINAL FIRST") states the obstacle and the first step without revealing the device cache-clear trick, and the in-world aside reinforces "reach the terminal, then make me let go." Escalation is layered: pre-arm hintText points to the ledge, a stray purge before arming triggers a warning haptic + terminal flash, and only after arming does the concrete "FREE UP STORAGE / CAN'T DO THIS?" instruction surface.
SUGGESTION: Fix the iPad composition so it actually does what the comments claim: clamp the climb into a real vertical column hugging size.width/2 (verify ipadClimbCenterX/offset math — it is rendering as a wide diagonal, not a confined ±48 column), and guarantee the terminal ledge near the ceiling, the full-height junk wall, and the EXIT are all on-screen in the single resting frame. Add an automated wide-canvas audit asserting junk-wall bounds, terminal ledge, and exit door are within the visible viewport (since the camera no longer follows) so this void can't regress silently.
