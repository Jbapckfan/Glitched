Level 2 — Microphone (blow) — extend a wind-bridge across an un-jumpable chasm
VERDICT: polish (4/5)
STRENGTHS: The blow-to-extend mechanic is genuinely fair: the chasm is deliberately un-jumpable (235pt, explicitly widened to kill the running-jump bypass) so the bridge is unmistakably the only route, and the crossing is forgiving thanks to a boosted power curve plus asymmetric lerp (fast extend, slow 18pt/s retract) that leaves ample time to walk after one good breath. The wind theming (turbines, vents, hatched chasm, animated gust placard) sells the fiction before any text does.
CONCERNS: The iPad render is the weak point: despite an elaborate isWideCanvas full-height tier-climb in source, the screenshot shows the flat phone layout pinned to the bottom quarter with the upper two-thirds an empty void — the composed climb isn't appearing, so the gap/overlap rules are moot and the screen reads unfinished. On phone the layout is clean and the chasm/bridge geometry is sound and completable.
CLUE/HINT: t=0 is well-pitched: the persistent placard says only "LOOKS WINDY" with an animated gust glyph (diegetic, never names the mic), so the breath trick stays unspoiled. Escalation is real and tiered — death triggers notePlayerStruggle, then difficultyHintDidShow surfaces a "BLOW" mic placard with sound waves, and hintText() backs it with "Try blowing into the microphone."
SUGGESTION: Fix/verify the iPad path so the composed tier-climb actually renders on portrait iPad (the screenshot still shows the bottom-anchored phone strip with a large empty band) — confirm the isWideCanvas gate trips on real device sizes, or fill the void with the intended ascending route so the chasm/bridge finale frames near the top instead of leaving two-thirds of the screen blank.
