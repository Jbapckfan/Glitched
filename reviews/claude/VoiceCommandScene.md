Level 22 — Speak BRIDGE / OPEN / FLY (in order) to extend a bridge over an un-jumpable chasm, unlock a door, then briefly fly to the exit ledge.
VERDICT: ship (4/5)
STRENGTHS: Genuinely novel voice mechanic with three obstacles each labeled by the exact word above it; FLY is correctly gated behind BRIDGE+OPEN with an in-scene correction, so the ordered solution can't be short-circuited. Strong accessibility: working no-mic button fallback plus the universal hatch.
CONCERNS: The transient timing windows (5s bridge retract, 2s FLY) aren't telegraphed at t=0, so a first-timer who walks too slowly after BRIDGE can get silently stranded mid-chasm and not understand why. iPad tier step is ~83pt against the 85 rise cap — safe but only ~2pt margin. Finale is off-screen right on the iPad (camera-follow), so the screenshot only shows the climb.
CLUE/HINT: t=0 is well-judged: the panel says only "SAY THE WORD" while the specific commands live on-course at each obstacle, so the trick isn't spoiled up front. Escalation is real — each death calls notePlayerStruggle() and hintText() eventually reveals the full ordered solution (BRIDGE, then OPEN, then FLY last).
SUGGESTION: Surface the bridge's 5s retract timer visually (e.g., the bridge alpha fading out as it nears retraction, or a brief "BRIDGE HOLDING…" countdown), so a player who hesitates mid-chasm understands the failure instead of dying to an invisible clock.
