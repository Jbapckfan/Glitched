Level 16 — Shake to rewind 3s (limited charges)
VERDICT: polish (4/5)
STRENGTHS: Genuinely required mechanic: the forced rotten final platform collapses and strands Bit on a catch ledge with the exit ~128pt up (past the ~91pt apex), so shake-to-undo is load-bearing, not cosmetic. Completability is rigorously guarded — the trap-repair undo is free, undoCount is floored to 1 (softlock guard), the inert exit gates behind repair, and preTrapAnchor gives a safe rewind even on a late undo. iPhone layout is clean and well-filled.
CONCERNS: iPad layout has a large dead void: the composed full-height climb renders crammed into the bottom ~40% with the entire top half (below the instruction panel to ~y=1100) empty white space — the ipadCeilingY = topSafeY-150 / tier math is not lifting the route high enough, so the "dead-sky fix" it documents is partially unmet here. The mechanic verb is never reachable through escalating help from a stuck-on-the-ledge player except via the 1.25s JIT prompt: the non-lethal trap means notePlayerStruggle (death-gated) never fires while stranded, so the JIT prompt is the sole on-ledge nudge — fine if it always fires, but it is the only line of defense.
CLUE/HINT: Yes — t=0 panel ("REGRET HAS A GRIP. USE IT / MISTAKES CAN BE UNMADE / BUT NOT FOREVER") is atmospheric and never names "shake," preserving the discovery. A stuck player gets strong escalation: a 1.25s post-collapse "SHAKE TO UNDO" JIT prompt (VoiceOver-spoken), plus the explicit death-gated hintText() naming the gesture and the top-left undo counter.
SUGGESTION: Fix the iPad vertical fill: raise ipadCeilingY (it is anchored too low relative to the actual rendered top) and/or verify ipadFillTierCount is producing enough tiers so the PEAK/FINALE land near the screen top, eliminating the empty upper half seen in the iPad capture. Re-screenshot to confirm the climb spans floor-to-ceiling like the iPhone path does.
