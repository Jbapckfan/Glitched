Level 20 — Walk Bit INTO the corruption wall to trigger a (simulated) system purge that clears the blocks and opens the exit.
VERDICT: polish (4/5)
STRENGTHS: Robust dual trigger (physics contact in didBegin + proximity-0.6 fallback) makes the purge fire reliably; the original "delete the app / fake review" gimmick was wisely converted to a self-contained simulated purge that is App Store safe. iPad is a genuine bottom-to-top climb with camera-follow and anchored HUD, and all gaps (≤130) / rises (≤85) sit inside Bit's budget. Strong A11Y: announceObjective + Reduce-Motion guards on the block flicker/glitch.
CONCERNS: NOTE: both screenshots caught the ~6.5s ominous-intro flicker ("RECOMMEND: FULL SYSTEM PURGE") over black, so the playable course is not visible in either capture — layout verified from source, not pixels. The escalating-help path is death-driven (notePlayerStruggle fires only on handleDeath), but this puzzle's real failure mode is inaction: the phone path is a flat overlapping floor with no hazard before the wall, so a player who just stops at the wall face never dies and never unlocks hintText. The post-purge exit gap is exactly 130 (at the cap, zero margin).
CLUE/HINT: Yes — the t=0 panel ("CORRUPTED DATA BLOCKS THE EXIT / IT WANTS YOU TO TURN BACK. EVERYTHING DOES.") frames the wall as something to confront without spoiling that you walk into it. hintText() gives a clean explicit reveal, but it only escalates on death, which this hazard-free approach rarely produces.
SUGGESTION: Decouple the escalating hint from death: add a stall/idle timer (e.g., Bit pressed against the wall face or no forward progress for ~8-10s) that calls notePlayerStruggle(), so a confused-but-alive player who stops short of the wall still earns the "push INTO the blocks" hint.
