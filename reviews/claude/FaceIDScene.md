Level 19 — Authenticate (Face ID / proximity) to clear two sequential biometric vault gates; the exit body is trapped behind door2's blocker so the second scan is mandatory.
VERDICT: polish (4/5)
STRENGTHS: The two-stage "FACE CHANGED - RESCANNING" escalation and the trap where the exit hides behind door2's blocker is a genuinely clever, well-coupled twist, and the imposter alarm (red flash + door shake + error haptic, with a Reduce-Motion soft-fade) sells the fiction. The decline-count software fallback (declinesBeforeFallback=2 -> forceHardwareFallback) is excellent: it keeps biometrics as the real gate yet guarantees no hard softlock for a player who cancels Face ID.
CONCERNS: iPad layout is the weak point: the screenshot shows gameplay crammed into the bottom ~40% with a large empty void between the centered modal and the staircase, and door1/the finale are scrolled off the right edge - the "fills full iPad height" claim isn't borne out because the camera Y stays centered on a low-spawned Bit. Mechanic clarity also lags: t=0 only mentions one vault, so the existence of a SECOND sequential gate (and that the exit is trapped behind it) is undiscoverable until after step 1, when "TAP THE NEXT GATE" first appears.
CLUE/HINT: t=0 is well-judged: "TAP IT TO SCAN YOUR FACE" teaches the verb without spoiling the multi-scan twist. A stuck player is covered by the base wall-clock no-progress net (~22s) surfacing hintText() "Authenticate identity"; status labels ("ONE MORE SCAN", "TAP THE NEXT GATE", "USE ON-SCREEN ID BUTTON") escalate well, but the level never calls notePlayerStruggle(), so it leans entirely on the generic fallback rather than a faster death/struggle-driven hint.
SUGGESTION: On the iPad composed path, raise the follow-camera so the centered modal sits above the gameplay band (or lower the spawn band) to kill the mid-screen void, and add an early on-screen marker/arrow toward door2 once door1 opens so the two-gate structure is legible before the player walks into the trapped exit.
