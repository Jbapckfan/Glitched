Level 6 — Raise device brightness to make UV "ghost" platforms solid (invisible <0.2, ghostly <0.5, fading-in <0.8, fully solid >=0.8); pushing to >=0.95 lights a sun/burn hazard that kills, so the goal is the high-but-not-max window.
VERDICT: ship (4/5)
STRENGTHS: The core loop is legible and self-teaching: platforms fade from dashed ghosts to glowing solids as brightness rises, and the on-screen LUX bar + indicator gives continuous physical feedback tied to the real hardware slider. The burn ceiling is genuinely well-staged — the safe band on the LUX bar stays hidden until the first burn (revealSweetSpotBand), turning the ceiling into an earned discovery rather than a printed rule. Jump budget is clean on both canvases (phone zig-zag ~16pt edge gaps / 58pt rises; iPad path explicitly capped at 130 e2e / 78 rise).
CONCERNS: The two-line t=0 whisper on iPhone visibly overlaps the gameplay column ("THE DARK IS HIDING THE FLOOR FROM..." prints across the UV platforms and clips the canvas edge in the iPhone shot), undercutting the careful upper-band placement code — the wrap/centering looks off on this render. The mechanic relies on the player intuiting that the LUX bar maps to the OS brightness slider; nothing at t=0 connects "BRIGHTNESS / up-arrow" to the burn risk, so a first-timer may slam to 100% and eat a death before the system teaches the ceiling. iPad layout fills the full height well with no void/overlap.
CLUE/HINT: Yes on both counts: t=0 uses atmosphere ("THE DARK IS HIDING THE FLOOR FROM BOTH OF US") that frames the puzzle without naming brightness or the burn trick, and a stuck player escalates correctly — 2 deaths/struggles plus 8s of play fires hintText, which finally spells out "raise brightness... but pull back before full." Death-driven notePlayerStruggle plus progress-reset on crossing 0.6 is a sound escalation curve.
SUGGESTION: Fix the iPhone t=0 caption so it never paints over the climb: the rendered text clips the right edge and sits on the platforms despite the upper-band logic — verify wrapTease against the actual compact width and consider lifting the band higher or shrinking font, since the de-spoil line currently reads worse than the iPad narrator path it was meant to improve on.
