Level 29 — "No gimmick, just walk" lie: you traverse right to a fake EXIT, the OS glitches and reveals the real exit was hidden back at your spawn — walk left to win.
VERDICT: ship (5/5)
STRENGTHS: The twist is genuinely well-built: the t=0 panel and subtitle sell the lie without leaking it, the reveal sequence (boss-register narrator, door shake, "GO BACK" arrow + chevron breadcrumb pinned left, fade-in real door) clearly redirects the player, and the player-analysis/trust beat ties the hesitation tracking back into the theme. iPad gets a real full-height diagonal staircase that fills the canvas top-to-bottom with no void.
CONCERNS: iPhone reach is safe — platforms 80..920 then lastPlatformX have edge-to-edge gaps ~30-90pt and rises <=30pt (well under 130/85); iPad climbs one tier/beat so each rise is <=85 by construction. iPad's per-tier overlap on tall devices only shrinks gaps. Main worry is the long, empty, breadcrumb-free walk-BACK from fake exit (~fakeExitX) all the way to spawn x=80: with platforms only "shaken" (not removed), the return trip is uneventful and could read as "is this broken?" before the real door scrolls into view.
CLUE/HINT: Yes — t=0 ("NO GIMMICK. JUST WALK") is the bait and never hints the exit is behind you, preserving the trick. Escalation works: deaths call notePlayerStruggle() and the earned hintText() asks "Are you sure the exit is ahead?" — a nudge toward the twist that still doesn't spoil it; revealTruth() calls notePlayerProgress() to reset the counter.
SUGGESTION: Add a single mid-course breadcrumb on the walk-back — e.g. a faint "<<" marker (or briefly re-light the real-exit door's glow) roughly halfway between fakeExitX and spawn — so the return trip has a confirming waypoint and never feels like a dead/empty corridor before the camera reaches x=80.
