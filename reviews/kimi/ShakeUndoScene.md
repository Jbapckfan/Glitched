Level 16 — Shake Undo

The opening text "REGRET HAS A GRIP. USE IT." withholds the verb, so the first encounter is discovery-driven, and help escalates sensibly via the base-class no-progress hint and `hintText()` naming shaking, plus `notePlayerStruggle()` on death and `notePlayerProgress()` during trap repair. Platforming is safe: the iPhone path's worst rise is 75 pt and widest gap ~60 pt, while iPad tier steps clamp to `maxJumpableRise` and authored advances stay ≤130 pt center-to-center. Yet the implementation is brittle: `isWideCanvas` hardcodes `min(size.width, size.height) >= 700` (line 36) despite a comment about 760 pt, `finalPlatformSurface` is recovered via `children.first as? SKShapeNode` in both build paths (lines 321, 475), and `breathTopHalf` is hardcoded to 12.5 (line 443) instead of deriving from the 25-pt beat height. `undoLabel.fontName = "Menlo-Bold"` (line 566) also bypasses the project font constant, and `tierCount` is clamped twice—first to 16, then to 13 (line 395)—burying the real limit. The most concrete fix is to refactor `createPlatform` to return or explicitly store the surface shape, removing the `children.first` dependency that silently breaks the glitch telegraph and trap reset.
