Level 14 — Focus Mode

The moon icon and "FOCUS TO OPEN" exit cue make the Do-Not-Disturb trick readable, but the iPad composed climb is long enough that one fall sends Bit back to the floor, so a single mistimed jump under hazards feels costly. Reach is generally safe: iPhone gaps sit around 20–30 pt horizontal and 50 pt vertical, and the iPad tier step is clamped to `BaseLevelScene.maxJumpableRise` (85) with authored gaps under 130 pt. The line-527 comment promises a ~50 pt left landing lane, yet the left patrol defined at line 536 as `(finaleLeft + 70, span: 60)` sweeps back to `finaleLeft + 10`, leaving only 10 pt of clearance and contradicting the guarantee. The opening text ("THE NOISE NEVER STOPS... UNLESS YOU LET IT.") avoids spoiling the solution, and `hintText()` plus the door cue eventually spell it out, but `notePlayerStruggle()` is never overridden, so struggling players only get the base-class ~18–22 s fallback instead of a death-specific nudge toward Control Center's crescent moon. Code quality slips with `calmOverlay!` (line 600) and `exitBlocker!` (line 681) force-unwraps, and `presentLockedExitCue()` throttles via `focusCuePresenting`, which only clears after the full 1.5 s fade, so repeated door contacts can show nothing. Improve this by moving all camera-anchored HUD setup into a method called after `installCameraFollow()` to ensure `gameCamera` exists before parenting `moonIcon` and `calmOverlay` to it.
