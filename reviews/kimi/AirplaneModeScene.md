Level 17 — Airplane Mode

The top-right plane icon labeled "OFF" and the platform airplane glyphs give players a clear target to experiment with, and the t=0 text avoids naming the toggle outright, so the first encounter is fair. Reach is within budget on both paths: the phone course keeps rises around 60 pt and gaps under 25 pt, and the iPad course clamps tier rises to ≤85 and horizontal steps under 70 pt, though the 50 pt tension-peak platforms leave only 3 pt clearance on each side of Bit's 44 pt body. `hintText()` eventually spells out the answer, but the escalation jumps straight from poetic panel text to a full Control Center tutorial with no intermediate nudge and no `notePlayerStruggle()` override to pace help. Code rough edges include the hardcoded `isWideCanvas` threshold (line 54), `setupBackground()` hardcoding the cloud span to 1945 (line 87) instead of sharing `composedCourseWidth`, and `platformDelayOffsets` only covering three of twelve flyers (line 29), which kills the cascading stagger. The `flyingPlatformSupportingBit()` debounce also swallows deliberate OFF toggles while Bit stands on a flyer (line 653), making input feel unresponsive. Concretely, share `composedCourseWidth` with the background, extend the delay array to all 12 flyers, and add a `notePlayerStruggle()`-driven mid-tier hint so help escalates instead of slamming from poetry to a tutorial.
