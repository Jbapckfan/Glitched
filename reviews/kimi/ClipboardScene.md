Level 12 — Clipboard

The clipboard "copy `GLITCH3D` elsewhere and return" mechanic is telegraphed by the terminal tease ("IT REMEMBERS WHAT YOU LAST HELD"), but the t=0 instruction panel only says "EXTRACT & RETURN" and never explicitly tells a first-timer to leave the app, so many will hunt for an in-game password before thinking of the OS pasteboard. Platforming stays within budget on both paths: iPhone platforms are 120–200 pt wide with the 145 pt locked door (top at `groundY + 122.5`) sitting ~16.5 pt above Bit's ~91 pt apex. On iPad, the composed climb uses a 118 pt center pitch and `verticalTier` rises clamped to the 85 pt budget, so every edge-to-edge gap and top-to-top rise is safe. The atmospheric terminal text avoids spoiling the answer, yet `hintText()` at line 735 immediately names the password and the full solve flow. Worse, the same spoiler string is returned on every call, so the death-triggered `notePlayerStruggle()` at line 718 cannot graduate from vague nudges to concrete help. Code rough edges include `clipboardScanLabel` at line 24 as dead storage, `doorBlocker!` force-unwrapped at line 396, and `isWideCanvas` at line 64 hard-coding a 1000 pt height threshold instead of using the base-class canvas helpers. One concrete improvement is to replace the single spoiler `hintText()` with a struggle-indexed hint table: first suggest checking the terminal's PASTE button, then hint at copying from another app, and only reveal `GLITCH3D` after repeated deaths or idle time.
