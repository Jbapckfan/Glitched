Level 18 — App Switcher

The t=0 text ("THE WORLD HOLDS ITS BREATH...") avoids outright spoiling the mechanic, yet the first encounter is unfair because the player must discover mid-air that pausing reveals hidden platforms with no safe practice space. iPhone geometry is safely within budget, yet the iPad zig-zag at lines 232–244 alternates 81 pt and ~169 pt center-to-center horizontal gaps, exceeding the 130 pt edge-to-edge limit and making every other jump unreachable. Once the static fallback expires, hintText() only repeats the same swipe-up instruction, and there are no escalating struggle prompts via notePlayerStruggle() to diagnose whether the player is stuck on the timing, the hidden platforms, or the iPad gap. Code rough edges compound this: setupLevelTitle, createPeekOverlay, and showInstructionPanel all test gameCamera (lines 112, 444, 503) before installCameraFollow creates it at line 82, so the title, freeze overlay, and instructions can attach to world space and scroll off-screen. Additionally, showTrajectoryLines hardcodes a 120 pt centered dashed line (lines 626–653) regardless of each spike's actual sweep range, and startDesyncedOscillation nearly always picks goingRight: true because phase < 1 is true for every value except measure-zero 1.0 (line 401). Fix the iPad path first by clamping the beat-to-beat horizontal distance to ≤130 pt, then reorder the scene flow so installCameraFollow runs before any UI attaches to gameCamera.
