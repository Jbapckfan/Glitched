Level 22 — Voice Command

The opening instruction panel only teases "SPEAK TO YOUR PHONE. SAY THE WORD." (lines 653–665), which is good, but the on-course labels immediately spell out "SAY \"BRIDGE\"", "SAY \"OPEN\"", and "SAY \"FLY\"" (lines 257, 270–271, 418–420), so the puzzle is solved before the player experiments. The 250-pt chasm is safely beyond the 130-pt jump budget on both paths (lines 37–39, 364–366), and the 100-pt exit rise is intentionally above the 85-pt cap (lines 243, 398), but the iPhone locked door keeps the default 90-pt height (lines 233, 479) while iPad raised it to 107 pt (line 386), leaving the iPhone route with a near-frame-perfect skip risk. `hintText()` dumps the full command order immediately (line 1123) rather than escalating, and `notePlayerStruggle()` (line 1109) is only fed on death with no differentiated guidance for players stuck on BRIDGE versus OPEN versus FLY. Several rough edges weaken the implementation: `doorBlocker!` is force-unwrapped in `createLockedDoor` (line 513), the instruction-panel fade relies on a brittle recursive `//permissionContinueButton` poll that never self-terminates if the base scene renames that node (lines 697–699), and the fallback buttons are pinned at a hardcoded y=50 without respecting `bottomSafeY` (line 931). Other hardcoded constants include the bridge auto-retract timer (5.0 s, lines 754–758) with no visual warning, the gravity/impulse values in `activateFly()` (lines 814–815, 821), and the `default` case in `presentFallbackControls()` (lines 932–936) silently mapping any unknown label to FLY. One concrete improvement: raise the iPhone door height to 107 pt to match the iPad margin, split `hintText()` into escalating nudges based on `notePlayerStruggle()` count, and replace the magic `//permissionContinueButton` probe with a delegate or notification from the base scene.
