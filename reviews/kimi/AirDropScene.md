Level 28 — Share Code

The opening instruction panel (lines 495–514) avoids spoiling the answer, but it also never gestures toward the pulsing SHARE button, so a first-time player is left to guess that the terminal is the actionable object; the Accessibility fallback at lines 853–859 reveals the code without explaining how the decode loop works, making simulator play feel like a bypass rather than a teach. Platforming is completable: the iPhone ground platforms at lines 176–178 overlap by about 35 pt, and the iPad switchback at lines 236–248 keeps edge-to-edge gaps under the 130 pt budget and tier rises around 83 pt under the 85 pt limit, so jump reach is safe. Hinting is thin: `notePlayerStruggle()` only fires on a wrong keypad submit (line 797) or death (line 940), and the sole `hintText()` override at line 954 dumps the full solution instead of escalating nudges for stuck players. The code is littered with brittle force-unwraps (`terminalScreen!` at line 320, `keyboardNode!` at line 621, `keyNode.name!` at line 882, and `alphabet.randomElement()!` at line 117) that will crash if setup order or key-name parsing ever drifts. Other rough edges include the share completion handler (lines 558–566) silently ignoring `completed == false`, leaving a cancelled sheet with no decoded text, no keypad, and no hint escalation, plus `isWideCanvas` at line 84 hard-coding a size threshold instead of using a base-class helper. One concrete improvement: route both cancelled shares and repeated struggles into a progressive hint ladder surfaced by `notePlayerStruggle()` counts, and replace the single `hintText()` reveal with stepped nudges so confused players earn the answer instead of receiving it all at once.
