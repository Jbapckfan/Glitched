Level 8 — Dark Mode

The moon/sun icons, ghost dashed outlines, and moon sensor above the door telegraph the mode-swap gimmick, and the t=0 panel's "NIGHT AND DAY DON'T AGREE..." / "THE DOOR ONLY TRUSTS THE DARK" hints (lines 1175/1195) stop short of naming Control Center, so the first encounter is fair. On iPhone, `pitch = min(w * 0.18, 90)` and `dualW = max(80, pitch * 0.85)` keep adjacent platforms overlapping, but `buildComposedIPadLevel()` hardcodes `pitch = 200` with `alwaysW = 130`/`dualW = 100`, yielding an 85pt edge-to-edge gap and an 85pt `tierStep` on every single beat—every jump sits at the exact ceiling of the safe budget for the entire climb. `handleDeath()` calls `notePlayerStruggle()` (line 1683), yet the overridden `hintText()` (line 1712) always returns the same full-spoiler string, so repeated struggle never escalates; the t=0 text teases correctly, but the fallback path dumps the entire solution at once. At line 687 the `.cluster` beat is materialized with `w: alwaysW` (130pt) even though the design comment at line 657 calls it a "wide ledge" paired with the 170pt `.rest`, deadening the intended pause beat. The shadow enemy on iPhone is misaligned: `createShadowEnemy()` positions it at `light1Point.x - 20` (line 1234) and runs a ±40pt patrol (lines 1271–1272) around a 14pt-radius hazard body (line 1263), sending the hazard 20pt past the left edge of the 80pt-wide light platform so it can hit Bit while falling beside it. Replace the single `hintText()` return with a struggle-counted escalation and relax the iPad `pitch` or cap `tierStep` so the whole ascent is not pinned at 85/85 on every jump.
