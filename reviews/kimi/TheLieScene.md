Level 29 — The Lie

The opening panel delivers the premise without spoiling the twist (lines 499-506), but first-timers have no visual reason to doubt the fake exit, so the reveal reads as an ambush rather than a puzzle. Jump reach is technically legal yet punishing: the iPhone segment’s 50 pt edge gap with a 30 pt rise already consumes most of Bit’s air, and the oscillating spike at x=600 sweeps the exact same landing window, turning the margin into a rhythm tax. On iPad the camera clamp stops at `levelWidth - size.width/2` (lines 777-780), leaving the final approach to `fakeExitX` completely off-screen, so players cannot even see what they are walking toward. Hints stay bland: `hintText()` asks “Are you sure the exit is ahead?” (line 879), and while deaths call `notePlayerStruggle()` (line 862), `hesitationCount` is tracked yet never escalated into a more specific nudge. Code quality is sloppy: `triggerFakeExitReveal()` disables the player via `playerController.cancel()` (line 539) without re-enabling control after the cutscene, which will strand anyone who waits too long; `setupLevelTitle()` hardcodes `"LEVEL 29"` (line 95) instead of using `levelID`; and the 3.8 s reveal duration is an unexplained magic number. Clamp the camera to `fakeExitX - size.width/2 + padding` so the lie is actually visible before the player commits to it.
