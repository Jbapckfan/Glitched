Level 23 — Battery Percentage

The opening panel hints at the drain mechanic without spoiling the hidden exit, and the simulator drain button gives non-device players a viable path, so first encounter is fair. iPhone reach stays inside budget: stone spacing clamps to at least 20 pt, vertical modulation tops out at 30 pt, and the hidden landing is only 100 pt below the 50% stone; the iPad composed path likewise bridges every horizontal hop with connective platforms and keeps each tier step to one safe rise. Hint escalation is thin: `hintText()` returns a single static line and `notePlayerStruggle()` is only reached through `handleDeath()` (line 823), so a live-but-stuck player waits for the base-class fallback instead of getting targeted help. The instruction text avoids a direct spoiler, but it also never nudges the player toward draining once the fake exit is reached. Code rough edges include the force-unwrap of `simulatedBattery!` at line 726, the hardcoded 45×18 physics body in `updateBatteryState` at lines 627–629 that resets resized iPad stones to the wrong width, and the `isWideCanvas` gate at line 37 (`size.height > 1000 && size.width > designSize.width`) which also triggers iPad landscape even though the composed course is authored for portrait. A concrete fix is to persist each stone's authored width and call `resizeStone` when re-adding the physics body, and to add a no-progress timer that invokes `notePlayerStruggle()` after about 10 seconds of no grounded movement so hints reach stuck players before death.
