Level 21 — Device Name

The top instruction "FIND THE DOOR WITH YOUR NAME" and the narrator line "THE DOOR KNOWS TOO" give away the solution before the player inspects the doors, so the first encounter is more of a wait than a discovery. Platforming reach is within the safe budget—iPhone gaps stay around 5–20 pt with rises ≤30 pt, and the iPad climb peaks at 85 pt rise with ~123 pt gaps—but the finale bays are narrower than Bit's 44 pt body, only ~27 pt on iPhone (line 318) and ~40 pt on iPad (lines 458/466), so the player gets squeezed between ground-category pillars while trying to contact the exit. The `hintText()` override at line 1069 returns the same "Your device knows your name..." line forever, and while `notePlayerStruggle()` fires on death at line 1054, there is no escalating hint chain beyond the base-class 18–22 s fallback, so stuck players stall; the doppelgänger rejection labels run on a fixed timer and can even solve the puzzle before the player reaches the doors. Code identity is fractured across the file: it is named `Level23_DeviceName.swift`, the header comment says "Level 23," but `levelID` is `.world3, index: 21` and the title label reads "LEVEL 21" (lines 142 and 229), making bug tracing error-prone. There is also a risky simulator fallback: `resolveFallbackName()` reads `UIDevice.current.name` directly at line 180 and can return a non-uppercased raw string, while `normalizedName()` at line 953 uppercases everything else inconsistently. One concrete improvement: widen every door bay to at least 55–60 pt and trim the frames so Bit's 44 pt body has clean clearance, turning the final choice into a real decision instead of a physics squeeze.
