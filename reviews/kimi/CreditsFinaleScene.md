Level 30 — Credits Finale

The opening HUD panel at lines 642–649 frames the goal plainly—climb the credits, avoid bugs—so the first encounter is fair, but the single static `hintText()` at line 948 never escalates even though `handleDeath()` already calls `notePlayerStruggle()` at line 922. Because `hintText()` ignores the struggle counter, stuck players see the same one-line reminder after every death instead of progressively specific guidance. Reach is technically safe: the phone path fixes top-to-top rise at 76 pt (line 157, under the 85 pt budget) and keeps horizontal overlap on 110 pt-wide zigzag rungs, while the iPad path derives tiers from `maxJumpableRise` (line 315) and clamps `halfSpread` to 120 (line 304), below the 130 pt gap budget. The phone bug patrol is sloppy: `createBugs` (lines 492–519) spawns each bug at `xOffset = ±60` and then adds a `scurryRange` of 50, so the bug’s center can drift to ±110 from the platform center—past the 55 pt half-width of the 110 pt rungs—meaning bugs scurry in mid-air off the platform edges. `handleExit()` at lines 937–939 calls `succeedLevel()` (which already fires base victory effects) and then layers its own `playVictorySequence()` on top, duplicating haptics, audio, confetti, and slow-motion. Accessibility is also half-wired: both `bug` and `exit` are marked `isAccessibilityElement` (lines 576 and 612) but never set `accessibilityFrame`, so VoiceOver will focus them at stale screen locations as the camera scrolls. The cleanest fix is to let `handleExit()` rely on the base success path alone and clamp each bug’s scurry range to `platformWidth/2 - abs(xOffset)` so hazards stay on their rungs.
