Level 2 — Wind

The “LOOKS WINDY” placard and drifting gust lines give a fair first read of the mechanic, but the bridge only gains collision once `bridgeCurrentWidth` exceeds `10 * visualScale` (line 639), so weak breaths spawn visible planks Bit falls through—an opaque failure that punishes tentative first attempts. Reach is correctly unjumpable on iPhone: the finale chasm at `235 * layoutXScale` ≈ 213 pt on a 390 pt screen far exceeds the 130 pt safe budget, enforcing mic use, while the iPad composed route keeps per-tier rises within 85 pt and step gaps at 105 pt, making the vertical climb completable by design. Hint pacing is mixed: the t=0 “LOOKS WINDY” sign avoids spelling out the answer, yet the struggle path only fires generic `notePlayerStruggle()` on death and never escalates to “blow harder / closer to the mic,” leaving soft-blow players stuck without targeted coaching. Code rough edges include the hardcoded iPad gate `size.height > 1000 && size.width > 600` (line 84), the unused `isCompactPhoneLayout` property (line 26), the dead `drawHangingVibrationPickup` definition (line 386), and force-unwraps on `bit`, `playerController`, and `bridge`. The bridge body is also rebuilt every time the visible segment count changes (line 654), which is brittle and can drop Bit during extension, and `setupWindVisuals` scales particle spacing by `layoutXScale` even on the wide canvas, so gust lines can overshoot the finale chasm on iPad. A concrete fix is to make the bridge collidable from the first visible segment and add a level-specific struggle hint such as `hintText("Blow harder to extend the bridge.")` after one or two soft-blow deaths, eliminating both the invisible-floor confusion and the feedback gap.
