Level 20 — Meta Finale

The mechanic is clear thematically but unfair on first contact: the gate reads as a hard wall, and nothing in the t=0 text signals that walking into it is the intended resolution, so first-time players will likely backtrack rather than commit. Phone path is flat and trivial, while the iPad climb gaps stay within budget at 65–120 edge-to-edge and tier rises capped near 85 (lines 357–361), so raw jump reach is not the problem. Hints avoid spoiling the trick at intro, but escalation is weak: `handleDeath()` calls `notePlayerStruggle()` (line 1159) and `hintText()` eventually blurts the solution (line 1179), yet there is no mid-climb iPad-specific struggle hint, only the generic base-class fallback. Code rough edges: `peakIndex = min(7, top)` (line 461) collides with the loop `while idx < top` starting at `idx = 5` (line 465), so when `top = 7` the tier-7 peak is skipped and the 120-gap beat never spawns. `isWideCanvas` is hardcoded to `size.height > 1000 && size.width > 700` (line 96), which misroutes iPad Pro 11" landscape into the phone path. Worse, at lines 955–965 `hintLabel.run(...)` is immediately followed by `hintLabel.removeAllActions()`, cancelling the corruption-cleared animation before it can run. One concrete fix: relocate `removeAllActions()` before the new sequence and add an iPad-only upper-tier struggle hint that nudges players toward the gate after repeated falls.
