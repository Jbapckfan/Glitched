Level 24 — Storage Space

The mechanic is discoverable: the intro panel withholds the concrete purge action until the terminal is armed (lines 613-618), but it prematurely names the "PURGE TERMINAL," so the t=0 text spoils the objective rather than letting the player infer it from the environment. Platforming reach is within budget: the iPhone path has a roughly 20-point edge-to-edge gap and 60-point rise to the terminal ledge (lines 269-275), while iPad uses `verticalTier()` auto-clamped to 85 with a 96-point center-to-center zig-zag (lines 81, 96-99, 344-369). Hint escalation is poor: `hintText()` only toggles between two static strings (lines 1033-1038), and the level never calls `notePlayerStruggle()`, so stuck players fall back to the generic base-class guidance instead of level-specific help. There are several code rough edges. `armPromptLabel = panel.children.first as? SKLabelNode` at line 673 is always nil because the first child is the background `SKShapeNode`, making the `armPromptLabel?.removeFromParent()` guard at line 622 dead; the iPad comment at lines 514-516 is stale because `installCameraFollow` was removed (lines 858-864); and `isWideCanvas` hardcodes a 700-point threshold at line 81 with no documented rationale for in-between sizes. One concrete improvement: fix the stale comment, either remove the dead `armPromptLabel` field or make it track the actual label, and add a level-specific `notePlayerStruggle()` path that flashes the terminal after ~15 seconds and explicitly mentions the Settings/CAN'T DO THIS? escape hatch after ~30 seconds.
