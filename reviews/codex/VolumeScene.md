Level 4 — Device volume buttons

The "KEEP IT QUIET" clue and wolf/water staging make the volume-down idea fair, though the t=0 copy is close to naming the solution. There is almost no meaningful jump challenge on the active phone path, so completability depends on the volume state rather than platform reach. Deaths feed `notePlayerStruggle()` and the hint names Volume Down, which is the right escalation for a hardware-button puzzle. The most concrete bug is `isWideCanvas` being hardcoded to `false`, leaving the composed iPad branch, camera-follow setup, and related anchor logic dead. A second rough edge is that the no-hardware fallback defaults `currentVolume` low enough to make the hazard nearly solved. One concrete improvement: either delete the dead iPad branch or re-enable it, then give the player a visible safe-volume band before the quiet threshold shrinks.
