Level 20 — App deletion / simulated system purge

The corruption gate is understandable and the hint eventually makes the counterintuitive action explicit: push Bit into the blocks rather than avoid them. The iPad route is jump-budgeted, but the post-purge exit gap is documented as exactly 130 points, which leaves no margin for physics/contact variance. Deaths call `notePlayerStruggle()`, and clearing corruption records progress, so the shared hint path is well connected. The biggest rough edge is design drift: the guide says app deletion/reinstall, while the code now performs an in-app simulated purge and only keeps a keychain shortcut for prior clears. That is probably better for App Store viability, but it means the scene no longer maps cleanly to the intended real device feature. One concrete improvement: update the in-world copy and guide to "system purge simulation", and shrink the final 130-point gap to give a real completion margin.
