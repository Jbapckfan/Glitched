Level 31 — Device flashlight + phone tilt

The dark cave and flashlight/tilt icons make the mechanic discoverable without spelling it out, though some players may not realize the actual device flashlight must be toggled. The iPhone sections and the iPad vertical route include several explicit reach fixes, including the checkpoint moved off a pit and stalactite clearance raised to avoid head-hit softlocks. Deaths call `notePlayerStruggle()`, and the hint clearly explains flashlight on, upright beam, and flat-floor scan. The t=0 text avoids spoiling, but the scene depends heavily on hardware state and motion input; without those, ambient glow and exit glow may not be enough to teach the intended solve. A rough code edge is that key progress moments such as flashlight activation and checkpoint passage call `resetProgressTimer()` rather than `notePlayerProgress()`, so they may not clear an already shown hint consistently. One concrete improvement: call `notePlayerProgress()` on first flashlight-on, first useful tilt, and checkpoint advancement, then surface a hardware fallback only after those inputs fail.
