Level 17 — Airplane Mode toggle

The "let it rise" clue is fair and points at flight without spelling out Airplane Mode immediately. The moving platforms and composed iPad route are designed around safe approach jumps, while the mode toggle supplies the real traversal change. Deaths call `notePlayerStruggle()`, but enabling Airplane Mode does not appear to call `notePlayerProgress()`, so the shared hint timer may keep counting after the player has solved the key idea. A rough code edge is `flyingPlatformSupportingBit()`, which can keep a platform aloft while Airplane Mode is off to avoid a trap, but that creates an inconsistent visual/system state and can blur the rule. Another brittle detail is the hardcoded cloud span matching an assumed composed course extent. One concrete improvement: derive decorative spans from `composedCourseWidth` and record progress the first time Airplane Mode turns on.
