Level 26 — Device language / locale

The locale clue is strong: scrambled signs and "not in a tongue this device still remembers" imply language without bluntly saying the answer. The extra revert instructions are practical and do not fully spoil the solve, which is important for a settings-detour puzzle. Platforming is well constrained, with the wrong route dead-ending and the revealed route using safe 130-gap/85-rise geometry on the composed iPad path. Deaths call `notePlayerStruggle()`, and `unscrambleWorld()` calls `notePlayerProgress()`, so the hint flow is well wired. A rough edge is language comparison: the first baseline ignore compares raw `language == baseline`, while later checks lower-case strings, so regional identifiers like `en-US` versus `en` can still be misclassified. One concrete improvement: normalize locale identifiers to a primary language code before baseline comparison and changed-from-baseline checks.
